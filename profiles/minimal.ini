; PHPVM profile preset: minimal
; Managed by phpvm — edit freely. Switch with `phpvm profile use minimal`.
;
; For static runtimes the common extension catalog is compiled in (see manifest).
; Add other directives as needed; extension loading is not managed via this file for static.

memory_limit = 128M
display_errors = On
error_reporting = E_ALL
